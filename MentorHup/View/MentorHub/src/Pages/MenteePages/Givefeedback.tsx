import React from 'react';
import FeedbackModal from '../../components/SessionReview/SessionReview';

const Givefeedback = () => {
    return (
        <FeedbackModal  onClose={() => {}} onSubmit={() => {}} open={true}/>
    )
}
export default Givefeedback
